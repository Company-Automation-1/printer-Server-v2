---
description: Swagger 已开启 JSDoc，用 JSDoc 替代可替换的装饰器
globs: '**/*.{dto,entity,controller,vo}.ts'
alwaysApply: false
---

# Swagger JSDoc 规范

项目已开启 `introspectComments`，能用 JSDoc 表达的就不写装饰器。

## 支持的 JSDoc 标签

| 场景 | JSDoc                 | 映射                       |
| ---- | --------------------- | -------------------------- |
| 属性 | 首行注释              | `ApiProperty.description`  |
| 属性 | `@example value`      | `ApiProperty.example`      |
| 方法 | 首行注释              | `ApiOperation.summary`     |
| 方法 | `@remarks 文本`       | `ApiOperation.description` |
| 方法 | `@deprecated`         | 标记废弃                   |
| 方法 | `@throws {code} 描述` | `ApiResponse`              |

## DTO / VO / Entity 属性

```typescript
// ❌ 不用
@ApiProperty({ description: '用户角色', example: ['admin'] })
roles: string[] = [];

// ✅ 用 JSDoc
/**
 * 用户角色
 * @example ['admin']
 */
roles: string[] = [];
```

### 必须用装饰器的例外

| 场景     | 原因                   | 写法                                                 |
| -------- | ---------------------- | ---------------------------------------------------- |
| enum     | 联合类型无法推断       | `@ApiProperty({ enum: XxxEnum })`                    |
| nullable | 类型含 null 时无法推断 | `@ApiProperty({ nullable: true })`                   |
| 文件上传 | 需指定 format          | `@ApiProperty({ type: 'string', format: 'binary' })` |

```typescript
// enum 必须保留 @ApiProperty
/** 任务状态 */
@ApiProperty({ enum: TaskStatus })
status: TaskStatus;

// 文件字段必须指定 format
/** 头像文件 */
@ApiProperty({ type: 'string', format: 'binary' })
file: Express.Multer.File;
```

## Controller 方法

```typescript
// ❌ 不用
@ApiOperation({ summary: '创建任务' })
@ApiResponse({ status: 400, description: '参数错误' })
@Post()
create() {}

// ✅ 用 JSDoc
/**
 * 创建任务
 * @throws {400} 参数错误
 */
@Post()
create() {}
```

带详细说明与废弃标记：

```typescript
/**
 * 创建任务
 * @remarks 支持批量创建，单次最多 100 条
 * @deprecated 请使用 POST /tasks/bulk
 * @throws {400} 参数校验失败
 * @throws {404} 设备不存在
 */
@Post()
create() {}
```

**注意**：复杂响应 schema（如 `allOf`、`$ref`）仍需 `@ApiResponse`，JSDoc 无法表达。
